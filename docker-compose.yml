version: "3.9"
services:
  nginx:
    build:
      context: .
      dockerfile: NginxDockerfile
    ports:
      - "80:80"
  backend:
    build:
      context: .
      dockerfile: BackendDockerfile
    ports:
      - "3000:3000"
  

